# X Markdown Exporter 改进升级计划

本文档将 2026-08 代码审查结果整理为可执行路线。原则是先保证“不会导错、不会越界读取、失败可见”，再提升归档质量和工程效率。

## Phase 1：正确性与安全热修

- [x] 使用严格路由分类支持 `/{user}/status/{id}` 与 `/i/web/status/{id}`
- [x] 禁止在首页、搜索页和个人主页通过富文本数量推断文章
- [x] 目标状态 ID 不存在时失败关闭，不回退到任意第一条推文
- [x] 作者未知时禁止线程扩展
- [x] 转义正文、引用和卡片中的不可信 Markdown/HTML 文本
- [x] 限定图片占位符替换范围
- [x] 拒绝页面脚本生成的非可信导出、复制、刷新和设置事件
- [x] 修正隐私说明、Issue 地址和动态版本展示

验收标准：路由、错误目标、匿名线程、正文注入和占位符碰撞均有自动化回归测试。

## Phase 2：导出与媒体可靠性

- [x] 图片请求增加域名、重定向、MIME、25 MB 单图和 20 秒超时限制
- [x] 图片请求明确使用 `credentials: omit`
- [x] ZIP 默认保留原始 JPEG/PNG/GIF/WebP 数据
- [x] embed 保留 PNG/WebP 格式和 GIF 动画
- [x] embed 增加 40 MB 处理期硬上限并自动回退 ZIP
- [x] 部分图片失败时向用户显示非静默警告
- [x] 将字符串图片占位符完全替换为结构化内容块
- [x] 增加 3 路图片并发上限、跨后台取消操作和总体导出进度

验收标准：失败的媒体不会产生无效本地路径，超大响应不会无限占用内存，用户能识别不完整归档。

## Phase 3：自动化与发布工程

- [x] 增加零外部依赖的 Node 回归测试
- [x] 增加统一语法检查和 `npm run check`
- [x] 增加拒绝静默覆盖的可复现打包脚本
- [x] 增加 GitHub Actions 检查和打包冒烟任务
- [x] 校验 manifest/package 版本及运行时文件完整性
- [x] 增加可在真实浏览器运行的匿名 X UI/DOM fixture，并验证复制与下载编排
- [x] 将真实浏览器 fixture 与 MV3 扩展安装验证接入 Playwright 和 GitHub Actions 自动化测试
- [x] 增加发布前版本、CHANGELOG、ZIP 逐文件内容与 SHA-256 检查

验收标准：每次提交自动执行检查；发布包由脚本从明确白名单生成，不能误带开发文件。

## Phase 4：体验与长期架构

- [x] 增加 dialog/status 语义、焦点恢复、模式状态和减少动画支持
- [x] 移除未使用的 `activeTab` 与 `abs.twimg.com` 权限
- [x] 使用结构化 `PostDocument` 模型分离 DOM 提取和 Markdown 渲染
- [x] 建立集中式选择器适配层和页面结构版本
- [x] 使用 `chrome.i18n` 提供完整中英文界面、进度提示和 Markdown 元数据标签
- [x] 增加排除正文、账号、状态 ID 和完整 URL 的无遥测诊断报告

验收标准：提取逻辑不直接拼接 Markdown；DOM 变化可通过 fixtures 快速定位；键盘和读屏流程可完整操作。
