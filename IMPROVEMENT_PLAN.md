# X Markdown Exporter 后续升级计划

更新于 2026-09-01。当前界面重构与修复纳入 v1.8.0 GitHub 发布；历史变更见 [CHANGELOG.md](CHANGELOG.md)，复现条件和审查边界见 [CODE_REVIEW.md](CODE_REVIEW.md)。

## 本轮已交付

- 两入口共用明暗主题与克制的工具界面，精简说明和操作层级。
- 任务重连、跨标签进度隔离、复制取消、诊断互斥和持久警告。
- 严格定位目标帖子与长文范围，排除引用、头像和无关内容。
- 线程首批改为同作者且有明确回复 / 线程上下文才纳入，缺少证据时退回主帖。
- 递归保留长文与富文本包装容器中的标题、段落、引用、代码块、有序列表起点和嵌套列表。
- 互动统计首批改为按语义识别原生按钮与本地化数量，保留无语义旧结构的保守回退。
- 导出消息协议加入版本、任务 ID、启动时间和单调修订号，弹窗拒绝跨任务的延迟状态。
- `embed` / `zip` 增加单任务 500 张图片和 64 MiB 累计媒体字节预算，超限明确失败。
- 图片流式字节上限、取消原因处理、参数顺序修复。
- 窄窗口、键盘、延迟正文、中文 / 英文及主题预览回归。
- 发布物与当前源码白名单逐文件校验。

以下项目是下一阶段工作，按优先顺序推进。不要在此基础上先堆新功能或换框架。

## 1. 下一轮：线程准确性与统计边界

| 工作 | 实施范围 | 验收标准 |
| --- | --- | --- |
| 收紧线程归属（首批已交付） | 只纳入同作者且有 `Replying to` / `Show this thread` 上下文的续帖；无法确认时只导出主帖 | 同作者的无关帖子不混入；插入他人回复、懒加载、不同语言文案和重复节点各有夹具 |

段落、标题、有序列表、嵌套列表、代码块和统计按钮语义的首批实现已经交付；复杂表格、数学公式、编辑器专用节点以及实时 X 的统计文案仍需验收。在剩余线程用例稳定前，不应宣称“完整线程归档”，也不开放默认关闭的统计选项。

## 2. 随后：大文件与真实扩展集成

- [x] 给 `embed` / `zip` 增加单任务累计媒体预算和图片数量上限；达到上限时停止读取并给出明确提示。
- [x] 给后台增加跨标签并发护栏（最多 6 路）和 FIFO 可取消排队（最多 96 个）；记录高水位、完成等待和拒绝计数，队列满时明确回退到远程链接，避免每个标签各开三路后叠加失控。
- [x] 按文档身份管理图片请求，页面导航 / 标签关闭后主动回收；验证取消不会影响另一标签的新任务。
- [x] 用已安装 MV3 扩展验证真实后台图片请求；流式超限、重定向、MIME 错误、超时和取消由后台单元覆盖，仍需在用户可用的真实 X 页面复核。
- [x] 验证打包阶段的取消响应；ZIP 压缩通过进度回调检查取消，使用流式文件模式，并释放 embed 转 ZIP 的临时 Data URL；canvas 限制为 8 MP，仍需测量真实解码 / ZIP 峰值内存。

验收：长文多图、多个标签同时导出、错误 MIME、缺失 Content-Length、下载中关闭标签都有受控用例；失败后按钮恢复且没有部分损坏文件。

## 3. 再做：减少日常打扰

- [ ] 提供“仅详情页显示 / 隐藏悬浮按钮”，保留工具栏入口。
- [ ] 增加位置重置和键盘移动方式；不要让拖动成为唯一方式。
- [ ] 可选导出前预览：显示帖子范围、图片数量和部分归档风险；不把整个工作流改成向导。
- [ ] 只在有实际需求时增加作者、时间等开关，放入折叠选项，不恢复满屏说明卡片。
- [ ] 保留小圆角、少量中性色、短文案、单一主要操作；状态色不能变回大面积装饰。

验收：仅用键盘完成打开、选择格式、操作、取消、关闭与位置恢复；系统明暗和 200% 缩放下保持可读，关闭的面板不参与焦点导航。

## 4. 持续维护

- [x] 为消息协议加入版本、任务 ID、任务启动时间和单调修订号；后续继续收敛两处 UI 的状态分支。
- [ ] 测量大页面的检查耗时，再按路由与正文范围缓存摘要；隐藏标签页停止不必要扫描。
- [ ] 建立匿名 DOM 夹具采集流程，移除账号、正文和 URL 后再入库；不自动上传页面。
- [ ] 补无障碍树、对比度、强制颜色和缩放检查；截图只作人工复核，不以脆弱像素快照替代行为测试。
- [ ] 发布前在用户确认可使用的真实 X 页面验收推文、线程、长文、引用、多图及未加载状态。

保持原生 JavaScript 和现有文档模型，先拆清“提取 / 任务 / 视图”的职责。暂不引入后端、账号系统、遥测或新的生产依赖。

## 发布门槛

1. `npm run check:all` 全部通过。
2. 更新版本与 CHANGELOG；核对 manifest、package 和展示版本。
3. 在新的输出目录打包并校验，不覆盖既有发布物。
4. 重新加载本地扩展，在真实 X 页面核对正文、作者、来源和图片。
5. 审核中英文说明与真实截图；确认没有新增权限或未说明的数据处理。
6. 发布 ZIP / 商店更新是独立步骤；本轮没有执行发布。

不要把通过本地 fixture 等同于适配所有实时 X 页面。每个阶段按验收标准关闭，不以测试数量或功能数量代替结果。
